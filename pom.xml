<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>top.dlzio</groupId>
    <artifactId>dlz-db</artifactId>
    <name>dlz-db</name>
    <version>8.0.0</version>
    <packaging>pom</packaging>
    <description>DLZ-DB v8.0: 多模块数据库框架，支持 Spring 和 Solon</description>
    <properties>
        <!-- 完整 reactor 使用 JDK 17+ 构建；主库发行产物保持 Java 8 兼容 -->
        <java.version>8</java.version>
        <maven.compiler.release>8</maven.compiler.release>
        <build.java.version>17</build.java.version>
        <maven.compiler.version>3.15.0</maven.compiler.version>
        <maven.enforcer.version>3.6.3</maven.enforcer.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- slf4j 1.7.x + logback 1.2.x：兼容 Spring Boot 2.6（LogbackLoggingSystem 依赖 StaticLoggerBinder），
             且 slf4j 1.7 API 是 2.x 子集，dlz-kit 仅用基础 MDC API，二者兼容 -->
        <slf4j.version>1.7.36</slf4j.version>
        <logback.version>1.2.13</logback.version>
        <lombok.version>1.18.30</lombok.version>
        <jupiter.version>5.8.1</jupiter.version>

        <dlz-kit.version>6.7.4</dlz-kit.version>
        <dlz-db.version>8.0.0</dlz-db.version>

        <!-- 代码质量工具版本 -->
        <jacoco.version>0.8.11</jacoco.version>
        <spotbugs.version>4.8.6</spotbugs.version>
        <spotbugs.maven.version>4.8.6.6</spotbugs.maven.version>
        <!-- Only high-confidence SpotBugs findings block release builds. -->
        <findsecbugs.version>1.12.0</findsecbugs.version>
        <checkstyle.version>9.3</checkstyle.version>
        <pmd.version>3.21.2</pmd.version>

        <jedis.version>4.4.3</jedis.version>
        <sqlite.version>3.43.0.0</sqlite.version>
        <redisson.version>3.21.1</redisson.version>
        <mysql.version>8.0.32</mysql.version>

        <!-- 质量阈值 -->
        <jacoco.line.coverage>0.70</jacoco.line.coverage>
        <jacoco.branch.coverage>0.60</jacoco.branch.coverage>

        <jacoco.skip>true</jacoco.skip>
        <spotbugs.skip>true</spotbugs.skip>
    </properties>
    <modules>
        <module>dlz-db-core</module>
        <module>dlz-db-spring-boot-starter</module>
        <module>dlz-db-solon-plugin</module>
        <!-- 示例工程参与聚合构建，但不纳入 JaCoCo 覆盖率统计，也不执行 install/deploy -->
        <module>dlz-db-web-demos</module>
    </modules>
    <dependencyManagement>
        <dependencies>
            <!--  logs  -->
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-api</artifactId>
                <version>${slf4j.version}</version>
            </dependency>
            <!--  测试期日志实现（SLF4J 绑定）：1.2.x 兼容 Spring Boot 2.6（提供 StaticLoggerBinder），固定版本  -->
            <dependency>
                <groupId>ch.qos.logback</groupId>
                <artifactId>logback-classic</artifactId>
                <version>${logback.version}</version>
                <scope>test</scope>
            </dependency>
            <!--  lombok  -->
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
                <scope>provided</scope>
            </dependency>
            <!--  test  -->
            <dependency>
                <groupId>junit</groupId>
                <artifactId>junit</artifactId>
                <version>${junit.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>${jupiter.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>mysql</groupId>
                <artifactId>mysql-connector-java</artifactId>
                <version>${mysql.version}</version>
            </dependency>
            <dependency>
                <groupId>org.xerial</groupId>
                <artifactId>sqlite-jdbc</artifactId>
                <version>${sqlite.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
    <dependencies>
        <!-- 与 dlz-kit-parent 对齐的日志方案：
             slf4j-api 以 provided+optional 提供（编译期可用、不强制传递给下游，由使用方决定日志实现与版本）；
             logback-classic 仅 test 作用域（本工程测试时打印日志），不进入发布产物、不传递。 -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <scope>provided</scope>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
        <!-- 测试 -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>mysql</groupId>
            <artifactId>mysql-connector-java</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- 使用 JDK 17+ 构建，同时将主库编译为 Java 8 兼容字节码/API -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven.compiler.version}</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                </configuration>
            </plugin>

            <!-- 完整 reactor 包含 Spring Boot 3 Demo，因此构建环境至少需要 JDK 17 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>${maven.enforcer.version}</version>
                <executions>
                    <execution>
                        <id>require-build-jdk</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireJavaVersion>
                                    <version>[${build.java.version},)</version>
                                    <message>完整构建需要 JDK ${build.java.version} 或更高版本；主库发行产物仍兼容 Java 8。</message>
                                </requireJavaVersion>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Maven Surefire 插件 - 覆盖父 POM 的 skip=true 配置 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>2.22.2</version>
                <configuration>
                    <!-- 不跳过测试 -->
                    <skip>${skipTests}</skip>
                    <skipTests>${skipTests}</skipTests>

                    <!-- 复用 JVM，避免每个测试类都重新初始化 Spring -->
                    <forkCount>1</forkCount>
                    <reuseForks>true</reuseForks>

                    <!-- 保留 JaCoCo 的 argLine 参数 -->
                    <argLine>@{argLine} -Xmx1024m</argLine>

                    <!-- 包含的测试 -->
                    <includes>
                        <include>**/*Test.java</include>
                        <include>**/*Tests.java</include>
                    </includes>
                </configuration>
            </plugin>

            <!-- JaCoCo 测试覆盖率 -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <configuration>
                    <outputDirectory>${project.build.directory}/reports/jacoco</outputDirectory>
                </configuration>
                <executions>
                    <!-- 为所有子模块准备JaCoCo agent -->
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <!-- 为所有子模块生成独立报告 -->
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>check</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>${jacoco.line.coverage}</minimum>
                                        </limit>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>${jacoco.branch.coverage}</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>

                </executions>
            </plugin>

            <!-- SpotBugs 静态分析 -->
            <plugin>
                <groupId>com.github.spotbugs</groupId>
                <artifactId>spotbugs-maven-plugin</artifactId>
                <version>${spotbugs.maven.version}</version>
                <configuration>
                    <effort>Max</effort>
                    <threshold>Low</threshold>
                    <maxRank>14</maxRank>
                    <xmlOutput>true</xmlOutput>
                    <htmlOutput>true</htmlOutput>
                    <failOnError>true</failOnError>
                    <outputDirectory>${project.build.directory}/reports/spotbugs</outputDirectory>
                    <excludeFilterFile>${project.basedir}/../reports/spotbugs-exclude.xml</excludeFilterFile>
                    <plugins>
                        <plugin>
                            <groupId>com.h3xstream.findsecbugs</groupId>
                            <artifactId>findsecbugs-plugin</artifactId>
                            <version>${findsecbugs.version}</version>
                        </plugin>
                    </plugins>
                </configuration>
                <executions>
                    <execution>
                        <id>spotbugs-check</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- PMD 代码质量 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-pmd-plugin</artifactId>
                <version>${pmd.version}</version>
                <configuration>
                    <failOnViolation>true</failOnViolation>
                    <printFailingErrors>true</printFailingErrors>
                    <targetJdk>1.8</targetJdk>
                    <outputDirectory>${project.build.directory}/reports/pmd</outputDirectory>
                </configuration>
                <executions>
                    <execution>
                        <id>pmd-check</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- OWASP Dependency-Check 依赖安全扫描 -->
            <plugin>
                <groupId>org.owasp</groupId>
                <artifactId>dependency-check-maven</artifactId>
                <version>9.0.9</version>
                <configuration>
                    <format>ALL</format>
                    <outputDirectory>${project.build.directory}/reports</outputDirectory>
                    <failBuildOnCVSS>7</failBuildOnCVSS>
                    <suppressionFile>reports/dependency-check-suppressions.xml</suppressionFile>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <!-- JaCoCo 聚合报告 Profile -->
    <profiles>
        <profile>
            <id>jacoco-aggregate</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.jacoco</groupId>
                        <artifactId>jacoco-maven-plugin</artifactId>
                        <version>${jacoco.version}</version>
                        <executions>
                            <execution>
                                <id>report-aggregate</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>report-aggregate</goal>
                                </goals>
                                <configuration>
                                    <outputDirectory>${project.build.directory}/reports/jacoco-aggregate
                                    </outputDirectory>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <licenses>
        <license>
            <name>Apache License 2.0</name>
            <url>http://www.apache.org/licenses/LICENSE-2.0</url>
        </license>
    </licenses>
    <scm>
        <url>https://github.com/dingkui/dlz-db</url>
        <connection>scm:git:https://github.com/dingkui/dlz-db.git</connection>
        <developerConnection>scm:git:https://github.com/dingkui/dlz-db.git</developerConnection>
    </scm>
    <developers>
        <developer>
            <id>dk1983</id>
            <name>dingkui</name>
            <email>59461202@qq.com</email>
        </developer>
    </developers>
</project>
