# 1.2 安装配置 - Solon

## 环境要求

| 环境 | 版本要求 |
|------|---------|
| JDK | 8+ |
| Maven | 3.6+ |
| Solon | 3.x |

## Maven 依赖

```xml
<dependency>
    <groupId>top.dlzio</groupId>
    <artifactId>dlz-db-solon-plugin</artifactId>
    <version>8.0.0</version>
</dependency>
```

插件通过 Solon SPI 自动加载，无需手动配置 Plugin 类。但是应用必须在 Solon 容器中提供一个 `DataSource` Bean，插件获取到它以后才能完成初始化。

## yaml 配置

```yaml
# app.yml
dlz:
  db:
    logic-delete-field: deleted
    log:
      show-run-sql: true
      show-caller: true

```

DLZ-DB Solon Plugin 本身不会根据一段通用 `datasource.*` YAML 自动创建数据源。如果项目没有专门的数据源插件，请显式注册：

```java
@Configuration
public class DataSourceConfig {
    @Bean
    public DataSource dataSource() {
        HikariConfig config = new HikariConfig();
        config.setDriverClassName("com.mysql.cj.jdbc.Driver");
        config.setJdbcUrl("jdbc:mysql://localhost:3306/test?useSSL=false");
        config.setUsername("root");
        config.setPassword("123456");
        return new HikariDataSource(config);
    }
}
```

完整 Solon 集成说明参见 [4.2-Solon完整集成](../第04章-框架集成/4.2-Solon完整集成.md)。
