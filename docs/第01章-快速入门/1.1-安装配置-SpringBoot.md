# 1.1 安装配置 - Spring Boot

## 环境要求

| 环境 | 版本要求 |
|------|---------|
| JDK | 8+ |
| Maven | 3.6+ |
| Spring Boot | 2.x / 3.x |

## Maven 依赖

```xml
<dependency>
    <groupId>top.dlzio</groupId>
    <artifactId>dlz-db-spring-boot-starter</artifactId>
    <version>8.0.0</version>
</dependency>
```

Starter 已传递引入 Spring JDBC 和自动配置依赖。项目仍需按所用数据库自行引入 JDBC 驱动。

## yaml 配置

```yaml
# application.yml
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456

# DLZ-DB 配置（可选）
dlz:
  db:
     logic-delete-field: deleted
     use-db-sql: false
     helper:
       auto-update: false
       package-name: com.dlz.test.db.entity
     log:
        show-result: true
        show-run-sql: true
        show-caller: true
```

## 自动装配

Starter 同时提供 Spring Boot 2 的 `spring.factories` 和 Spring Boot 3 的 `AutoConfiguration.imports`。配置好 `spring.datasource` 后即可直接使用 `DB.*`，无需编写或继承 DLZ-DB 配置类。

